【需要新增的功能】
1. 收集册（印章表格）：
   - 新增“收集册”按钮，点击弹窗显示所有已获得的小动物。
   - 网格布局（如4x4），每个格子显示小动物头像 + 名称 + 获得次数（×N）。
   - 用户可以点击任意已获得的小动物，切换当前陪伴（替换右下角的小动物）。
   - 当前陪伴的小动物在收集册中有高亮标记。
   - 收集册数据从现有的小动物孵化记录中读取（如果你已有记录结构，请沿用；如果没有，请在 localStorage 中新建 collectedAnimals 数组）。

2. 便利贴颜色自定义：
   - 便利贴右上角增加调色板图标 🎨。
   - 点击后弹出预设颜色选项（淡黄、淡粉、淡蓝、淡绿、浅紫）。
   - 用户选择后便利贴背景色立即改变，并保存到 localStorage，刷新后保持。

3. 历史任务记录：
   - 新增“历史”按钮，点击查看以往所有日期的任务完成情况。
   - 以日历或列表形式展示：日期、当天完成的任务数/总任务数。
   - 点击某一天可以查看当天具体任务清单（只读）。
   - 历史数据按自然日存储（localStorage 中以日期为键）。你需要在我的代码中增加每日任务快照功能。

4. 未完成任务温柔顺延：
   - 每天首次打开便利贴时（检测日期变化），检查前一天是否有未完成的任务。
   - 弹出一个温和选择框：“昨天还有 X 个任务没做。你想移到今天，还是让它们消失？”
   - 选项：“移到今天”（将任务添加到今天的任务列表，重置完成状态），“让它们消失”（直接删除）。
   - 如果用户关闭弹窗，默认不处理。

5. 小动物对白升级（ADHD友好）：
   - 替换原有夸奖/安慰语，使用以下示例（可随机选择）：
     完成任务：“你从‘不想做’到‘做了’，这中间的努力只有你自己知道。”
     点击小动物：“今天不想做也没关系，我陪你发呆。”
     求安慰：“你没有失败，你只是在为‘能做到’的那天积蓄能量。”
     埋任务时（如果你有埋任务功能）：“这个任务太重了？我帮你埋进土里。”
   - 所有对白不使用感叹号连击，语气平静真诚。

6. PWA可安装（便利贴常驻桌面）：
   - 增加 manifest.json 和 service worker，使网页可以安装到桌面，离线可用。
   - 提供“安装”提示按钮或浏览器自动提示。

要求：
- 不要改变我现有的任务添加、删除、勾选、彩蛋孵化、小动物互动等核心逻辑。
- 只增加上述缺失的模块，并确保新旧功能数据都保存在 localStorage 中。
- 输出完整的 index.html（包含 CSS/JS），以及 manifest.json 和 service worker 文件（如果无法生成多文件，可以将 manifest 和 sw 内联或动态生成）。
- 保持像素风格，响应式。

请直接输出修改后的完整代码。